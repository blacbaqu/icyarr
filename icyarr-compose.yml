version: "3.9"

services:
  icyarr:
    container_name: icyarr
    build:
      context: ./icyarr
      dockerfile: ./Dockerfile
    ports:
      - "8000:8000"   # expose backend + static frontend
    volumes:
      - ./icyarr/channel.db:/app/channel.db
    restart: unless-stopped
